import { Component, Input, Output, EventEmitter } from '@angular/core';
import { Artiste } from '../shared/oneartiste.model';


@Component({
	selector: 'app-userchoice',
	templateUrl: './userchoice.component.html',
	styleUrls: ['./userchoice.component.css']
})
export class UserchoiceComponent {
	// @Input() artistes: Artiste[] = [
	// 	new Artiste('Praiz', 'PFA Graduate')
	// ];
}
